z// src/app/scheduler/page.tsx (example protected route - apply to all routes)
import { AuthCheck } from '@/components/AuthCheck';

export default function SchedulerPage() {
  return (
    <AuthCheck>
      {/* Your existing scheduler page content */}
    </AuthCheck>
  );
}
